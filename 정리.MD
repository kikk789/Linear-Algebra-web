# 면접용 최종 정리 — 내적·투영·선형회귀 (확장판)

## 1. 내적이 뭔가요?

두 벡터의 대응 성분의 곱을 모두 더한 값.

```
(2, 3) · (4, 1) = 2·4 + 3·1 = 11
```

의미: 두 벡터가 같은 방향으로 얼마나 가는지의 측정값.

---

## 2. 핵심 용어 — 길이, 투영, 내적

| 용어 | 영어/기호 | 값 | 의미 |
|---|---|---|---|
| 벡터 길이 (노름) | norm, \|v\| | \|v\| = √13 ≈ 3.6 | 원점에서 벡터 끝점까지의 거리 |
| 투영 길이 | projection, proj | proj = 3.05 | 원점에서 그림자 자리까지의 거리 (v 방향 직선 위) |
| 내적 | dot product, dot | dot = 11 | 정렬도 × 크기 |

관계:

```
내적 = 투영 길이 × 기준 벡터 길이
 11  =   3.05    ×    √13
```

\|v\| 와 proj 는 같은 직선 위, 다른 자리까지의 거리:

원점에서 (2, 3) 방향 직선을 따라가면
- 3.05 지점 = w의 그림자 자리 (proj 끝)
- 3.6 지점 = v의 끝점 (\|v\| 끝)

---

## 3. 전치 (Transpose)

열벡터를 행벡터로 모양 바꾸는 연산 (또는 그 반대).

```
v = ⎡2⎤   ─ᵀ→   vᵀ = [2  3]
    ⎣3⎦
(열벡터, 2×1)    (행벡터, 1×2)
```

기호: 위첨자 T, vᵀ로 표기.

**성질:**
- 행과 열을 맞바꿈
- 두 번 하면 원래대로: (vᵀ)ᵀ = v
- 모양 변경 (m×n → n×m), 숫자는 그대로

**왜 중요한가:**
- 곱셈 위치에 따라 역할이 바뀜
- 열벡터 (오른쪽) → "입력/점" 역할
- 행벡터 (왼쪽) → "함수/연산자" 역할
- 전치로 점(벡터)을 함수(행렬)로 모양 전환 → 쌍대성의 기술적 도구

우리 예시:

```
v · w = vᵀ w
(2,3) · (4,1) = [2 3] · ⎡4⎤ = 2·4 + 3·1 = 11
                         ⎣1⎦
```

좌변은 내적 표기, 우변은 행렬곱 표기. 같은 11.

---

## 4. 기하학적 의미 — 두 단계

쌍대 함수의 작동:
1. **투영**: w를 v 방향 직선에 수직으로 떨어뜨림 → proj = 3.05 (원점부터 수직으로 떨어진 지점까지의 거리)
2. **스케일**: 그 투영 길이에 |v| 곱함 → 11

수식:

```
v · w = (v의 길이) × (w의 길이) × cos(사이각)
       = |v|       × |w|      × cos θ
       = √13 × √17 × cos(42°)
       ≈ 11
```

---

## 5. 코사인 유사도 (정규화) <- 잘 모름

내적은 크기 영향을 받음. 순수 정렬도만 보려면 두 길이로 나눔:

```
cos θ = (v · w) ÷ (|v| × |w|)
      = 11 ÷ (√13 × √17)
      ≈ 0.74    (74% 정렬)
```

항상 −1 ~ 1 사이. 1이면 동일 방향, 0이면 직교, −1이면 반대.

---

## 6. 쌍대성 (Duality)

벡터 (2, 3)을 함수 f(x, y) = 2x + 3y 로도 볼 수 있음.

```
벡터 v   ─전치→   vᵀ (1×n 행렬)   ─적용→   점수 함수 f(x) = vᵀx = v·x
(좌표/점)         (행벡터)              (가중치 점수 함수)
```

- 같은 두 숫자가 좌표(점)이자 가중치(측정 기준)
- 점수 함수 출력 = 내적
- → 1:1 대응이 쌍대성. 전치는 이 관점 전환의 기술적 다리.

**용어:**
- **전치**: 모양 변환 (열↔행)
- **쌍대 함수**: 벡터에 대응하는 점수 함수
- **선형 범함수 (linear functional)**: ℝⁿ→ℝ 선형 함수 (정식 명칭)
- **쌍대 공간 (dual space)**: 모든 선형 범함수의 모임 (V*)

---

## 7. 영공간 (Null Space)

점수 함수 출력이 0이 되는 입력들의 집합.

```
v · x = 0 인 x들 = v에 직교하는 부분공간
```

예: v=(2,3)의 영공간 = (3, −2) 방향 직선 (v에 수직).

→ 선형 분류기의 결정 경계가 정확히 이 영공간.

---

## 8. 행렬-벡터 곱 (Matrix-Vector Product) ⭐ 신규

내적이 1:1 매핑이면, 데이터 m건 한 번에 처리는 **행렬-벡터 곱**.

```
       ⎡ x₁ᵀ ⎤         ⎡ x₁ · w ⎤   ⎡ ŷ₁ ⎤
X·w =  ⎢ x₂ᵀ ⎥ · w  =  ⎢ x₂ · w ⎥ = ⎢ ŷ₂ ⎥
       ⎣ x₃ᵀ ⎦         ⎣ x₃ · w ⎦   ⎣ ŷ₃ ⎦
```

- 행렬 X의 각 행이 한 데이터
- 곱 결과의 각 행이 그 데이터의 예측
- **행렬-벡터 곱 = 내적을 m번 묶어 한 번에 처리**

ML 코드에서 매번 등장 (`X @ w`, `np.dot(X, w)`).

---

## 9. 선형회귀 = 최소제곱법 = 투영 ⭐ 신규

이게 ML과 선형대수의 가장 본질적 연결.

**문제**: 데이터 점들에 가장 잘 맞는 직선 찾기.

```
모델: y = w·x + b
손실: J(w, b) = Σᵢ (yᵢ − w·xᵢ − b)²    (오차 제곱합)
학습: J 최소화하는 w, b 찾기
```

**두 시각의 동치성:**

| 방법 | 푸는 조건 | 식 |
|---|---|---|
| 최소제곱 (대수) | J 최소 = 미분 0 | Σ(y − wx)·x = 0 |
| 투영 (기하) | 오차 ⊥ X | (y − Xw) · X = 0 |

→ **같은 식.** 다른 출발점, 같은 결과.

**기하학적 의미:**

> y 벡터를 X 열공간에 수직 투영. 발 자리가 최적 예측, 그 발의 X에 대한 배수가 최적 가중치 w.

**한 줄 공식 (정규방정식, Normal Equation):**

```
w* = (XᵀX)⁻¹ Xᵀy
```

이 식 전체가 본질적으로 "투영"의 닫힌 해.

---

## 10. 학습 방법 두 가지 ⭐ 신규

| 방법 | 정규방정식 | 경사하강법 |
|---|---|---|
| 영어 | Normal Equation | Gradient Descent |
| 식 | w = (XᵀX)⁻¹ Xᵀy | wₙ₊₁ = wₙ − η·∇J |
| 풀이 | 한 방 (closed-form) | 반복 (iterative) |
| 적용 | 선형회귀 (식이 깨끗) | 신경망 등 비선형 |

**왜 두 방법인가:**
- 선형회귀는 J가 파라미터에 대해 2차식 → 미분 = 0 풀 수 있음 → 정규방정식
- 신경망은 비선형 활성화 합성 → 닫힌 해 없음 → 경사하강법 반복

**면접 키워드:**
- **닫힌 해 (closed-form solution)**: 한 줄 공식 존재
- **반복 최적화 (iterative optimization)**: 수치 근사
- **비선형성 (non-linearity)**: 변수가 식에서 분리 안 됨

---

## 11. 미분과 경사하강법 ⭐ 신규

경사하강법의 본질:

```
새 w = 현재 w − (학습률 η) × 미분값(∂J/∂w)
```

| 요소 | 의미 |
|---|---|
| 미분 부호 | 내리막 방향 (음수=우측 내리막, 양수=좌측 내리막) |
| 미분 크기 | 얼마나 가파른지 = 보폭 |
| 학습률 η | 보폭 배율 |

**산 등반 비유:**
- J 곡선이 산 모양 (이미 정해져 있음)
- 미분이 발 밑 경사 감각
- 내리막 쪽으로 한 발씩 → 결국 바닥(최저점) 도달

---

## 12. 가중치 학습 = 머신러닝

```
정답 있는 과거 데이터로 → 가중치 v 조정 → 정답에 가장 가까운 v 찾기
                              ↓
                          학습 완료
                              ↓
정답 모르는 새 입력 → 학습된 v로 점수 매김 → 예측/추천/분류
```

**정답 출처:**
- 사용자 행동 (클릭, 평점)
- 사람이 라벨링한 데이터
- 자연 발생 데이터 (다음 단어 등)
- 환경 보상 (게임 점수, 강화학습)

---

## 13. 적용 분야

| 분야 | 가중치 v | 입력 x | 점수 v·x |
|---|---|---|---|
| **선형회귀** | 회귀 계수 | 특성 | 예측값 |
| 추천 시스템 | 사용자 취향 | 영화 특징 | 추천 점수 |
| 검색 | 검색어 임베딩 | 문서 임베딩 | 관련도 |
| 분류 | 결정 기준 | 데이터 | 카테고리 점수 |
| 신경망 뉴런 | 학습된 가중치 | 이전 층 출력 | 활성화 |
| 어텐션 (LLM) | Query | Key | 주목도 |

전부 **가중치와 입력의 내적 구조**.

---

## 30초 면접 답변 (확장판)

> 내적은 두 벡터의 대응 성분을 곱해서 모두 더한 값으로, (2,3)·(4,1) = 2·4 + 3·1 = 11 입니다.
>
> 기하학적으로는 한 벡터를 다른 벡터 방향 직선에 떨어뜨려서, 그 직선 위에서 원점부터 그림자 자리까지 잰 거리(투영 길이, 3.05)에 기준 벡터의 길이(√13)를 곱한 양이며, 두 길이 곱과 사이각 코사인의 곱으로도 표현됩니다.
>
> 한편 v를 전치하면 vᵀ라는 1×n 행렬이 되고, 이를 입력에 곱하면 v·w와 같은 결과가 나옵니다. 이 행렬 관점에서 내적은 '벡터를 1×n 행렬로 재해석한 후 입력에 적용한 결과'이고, 이게 쌍대성의 핵심입니다. 벡터는 좌표이자 가중치 점수 함수 — 양쪽으로 볼 수 있다는 사실이죠.
>
> 순수 방향 일치도만 보려면 코사인 유사도 (내적 ÷ 두 길이 곱)로 정규화하고, 점수가 0이 되는 입력들이 가중치에 직교하는 영공간 = 선형 분류기의 결정 경계가 됩니다.
>
> **이 내적·투영 구조의 가장 기본 응용이 선형회귀의 최소제곱법입니다. 데이터의 y 벡터를 입력 행렬 X의 열공간에 수직 투영하면 그 발이 최적 예측이 되고, 정규방정식 (XᵀX)⁻¹Xᵀy가 이 투영의 한 줄 공식입니다. 손실 함수가 파라미터에 대해 2차식이라 닫힌 해가 존재하기 때문에 한 방에 풀립니다. 반면 신경망처럼 비선형 활성화가 여러 층 합성된 모델은 닫힌 해가 없어 경사하강법으로 반복 최적화합니다.**
>
> 머신러닝의 학습은 정답 데이터로 가중치를 조정해 좋은 점수 함수를 찾는 작업이고, 추천·검색·신경망·어텐션이 모두 이 가중 내적 구조를 공유합니다.

---

## 핵심 용어 9단어 (3개 추가)

- **노름** \|v\| = √13: 원점 → v 끝점까지 거리
- **투영 길이** proj = 3.05: 원점 → 그림자 자리 거리 (직선 위)
- **내적** dot = 11: 대응 성분 곱의 합 = 투영 × 기준 길이
- **코사인 유사도** = 0.74: 정렬 비율
- **전치** vᵀ: 열↔행 모양 변환 (점 → 함수 다리)
- **쌍대성**: 벡터 ↔ 점수 함수의 1:1 대응
- **최소제곱법 (Least Squares)** ⭐: 손실 = 오차 제곱합, 선형회귀의 학습 기준
- **정규방정식 (XᵀX)⁻¹Xᵀy** ⭐: 투영의 닫힌 해, 선형회귀를 한 줄로
- **경사하강법 (Gradient Descent)** ⭐: 미분 따라 한 스텝씩, 비선형 모델용

---

## 핵심 관계식 (확장)

```
내적 = 대응 성분 곱의 합
    = 투영 × 기준 길이
    = |v||w| cos θ
    = vᵀ w  (전치 후 행렬곱)

코사인 유사도 = 내적 ÷ (|v| × |w|)

점수 함수 f(x) = v · x  (쌍대성)

손실 함수 J = Σ (yᵢ − w·xᵢ − b)²       ⭐
정규방정식 w* = (XᵀX)⁻¹ Xᵀy            ⭐
경사하강법 wₙ₊₁ = wₙ − η · ∂J/∂w        ⭐
```

---

## 핵심 결론

머신러닝 = 정답 데이터로 좋은 가중치(쌍대 함수) 찾기.

전치로 벡터를 행렬로 보고, 그 행렬과 입력의 내적이 예측 점수.

**선형회귀는 이 내적·투영 구조의 가장 기본 응용. y를 X 열공간에 투영해서 최적 가중치를 닫힌 해로 도출. 비선형 모델은 닫힌 해 없어 경사하강법으로 반복 근사.**