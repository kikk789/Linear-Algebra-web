면접용 최종 정리 — 쌍대성 양방향 보강
1. 내적
두 벡터의 대응 성분의 곱을 모두 더한 값.


(2, 3) · (4, 1) = 2·4 + 3·1 = 11
2. 핵심 용어 — 길이, 투영, 내적
용어	값	의미
노름 |v|	√13 ≈ 3.6	원점 → v 끝점 거리
투영 길이 proj	3.05	원점 → 그림자 자리 거리 (직선 위)
내적 dot	11	정렬도 × 크기
관계: 내적 = 투영 × 기준 길이

|v| 와 proj — 같은 직선 위 다른 자리:

(2, 3) 방향 직선 따라가면 3.05 지점 = 그림자, 3.6 지점 = v 끝점
3. 전치 (Transpose)
열벡터 ↔ 행벡터 모양 변환.


v = ⎡2⎤   ─ᵀ→   vᵀ = [2  3]
    ⎣3⎦
행렬곱에서 왼쪽 위치 = 함수 역할 가능하게 함
점 → 함수 관점 전환의 기술적 도구
4. 기하학적 의미
두 단계:

투영: w를 v 방향 직선에 떨어뜨림 → proj = 3.05
스케일: 길이에 |v| 곱함 → 11

v · w = |v| × |w| × cos(사이각)
5. 코사인 유사도

cos θ = (v · w) ÷ (|v| × |w|) ≈ 0.74
크기 영향 제거. -1 ~ 1 사이.

6. 쌍대성 (Duality) — 양방향 1:1 대응 ⭐ 보강
쌍대성은 벡터와 ℝⁿ→ℝ 선형 함수의 양방향 1:1 대응:

방향 ① (벡터 → 함수):
모든 벡터 v는 점수 함수 f(x) = v · x 를 정의한다.


v = (2, 3) → f(x, y) = 2x + 3y
방향 ② (함수 → 벡터) ⭐:
모든 ℝⁿ→ℝ 선형 함수는 어떤 벡터 v와의 내적 형태로 표현된다.


임의의 선형 함수 g : ℝ² → ℝ
g가 표준기저에서 어떻게 작용하는지 보자:
  g(1, 0) = a   (어떤 숫자)
  g(0, 1) = b   (또 다른 숫자)

선형성에 의해 임의 (x, y)에 대해:
  g(x, y) = ax + by = (a, b) · (x, y)

→ g는 벡터 (a, b)와의 내적이다.
즉: ℝ²→ℝ 선형 함수를 아무거나 잡아도 → 항상 어떤 벡터와의 내적으로 쓸 수 있음. 그 벡터가 g의 쌍대 벡터.

1:1 대응:


벡터 v       ↔       함수 f(x) = v · x
(a, b)               g(x, y) = ax + by
정확히 한 벡터 ↔ 정확히 한 함수.

용어:

전치: 모양 변환 도구
쌍대 함수: 벡터에 대응하는 점수 함수
선형 범함수: ℝⁿ→ℝ 선형 함수 (정식 명칭)
쌍대 공간 (dual space): 모든 선형 범함수의 모임 V*
Riesz 표현 정리: "유한차원에서 모든 선형 범함수는 어떤 벡터와의 내적으로 유일하게 표현된다" — 방향 ②의 정식 정리명
7. 영공간
v · x = 0 인 x들 = v에 직교하는 부분공간.
선형 분류기의 결정 경계.

8. 가중치 학습 = ML
정답 데이터 → 가중치 v 조정 → 학습 완료 → 새 입력에 v로 내적 → 예측.

쌍대성 양방향이 ML에서 의미:

방향 ①: "가중치를 학습한다" = "벡터를 찾는다" = "그 벡터에 대응하는 점수 함수를 찾는 것과 같다"
방향 ②: "원하는 선형 함수가 있다" = "그것은 반드시 어떤 가중치 벡터로 표현된다" → 학습 가능성의 이론적 보장
9. 적용 분야
분야	가중치 v	입력 x	점수 v·x
추천	사용자 취향	영화 특징	추천 점수
검색	검색어 임베딩	문서 임베딩	관련도
분류	결정 기준	데이터	카테고리 점수
신경망 뉴런	학습된 가중치	이전 층 출력	활성화
30초 면접 답변 (양방향 포함)
"내적은 두 벡터의 대응 성분의 곱을 모두 더한 값으로, (2,3)·(4,1) = 11 입니다.

기하학적으로는 한 벡터를 다른 벡터 방향 직선에 떨어뜨려 잰 투영 길이(3.05)에 기준 벡터 길이(√13)를 곱한 양이며, 두 길이 곱과 사이각 코사인의 곱과 같습니다.

쌍대성은 벡터와 ℝⁿ→ℝ 선형 함수의 양방향 1:1 대응입니다. 모든 벡터는 점수 함수를 정의하고, 반대로 모든 ℝⁿ→ℝ 선형 함수는 어떤 벡터와의 내적 형태로 유일하게 표현됩니다. 후자는 표준기저에서의 함수값을 그대로 모은 벡터가 그 함수의 쌍대 벡터가 된다는 사실로 증명되며, 이를 통해 모든 선형 점수 매김이 가중 내적 구조로 통일됩니다.

머신러닝의 학습은 정답 데이터로 적절한 쌍대 벡터(가중치)를 찾는 작업이고, 추천·검색·신경망이 모두 이 구조를 공유합니다."

핵심 용어 6단어 + 쌍대성 양방향
노름 |v| = √13
투영 길이 proj = 3.05
내적 dot = 11 (= 대응 성분 곱의 합 = 투영 × 기준 길이)
코사인 유사도 0.74
전치 vᵀ
쌍대성: 벡터 ↔ ℝⁿ→ℝ 함수의 양방향 1:1 대응
① 벡터 → 함수: 모든 v가 f(x) = v·x 를 정의
② 함수 → 벡터: 모든 선형 함수가 어떤 v와의 내적으로 표현됨 (Riesz)
핵심 결론
머신러닝 = 정답 데이터로 적절한 가중치 찾기 = 적절한 쌍대 벡터 찾기.
모든 선형 점수 매김은 반드시 어떤 벡터와의 내적이다 (쌍대성 방향 ②).
그래서 가중치 학습이 보편적 도구가 된다.